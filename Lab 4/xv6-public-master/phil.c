#include "types.h"
#include "stat.h"
#include "user.h"
#include "fcntl.h"

int
main(int argc, char *argv[])
{
//     int i = atoi(argv[0]);

//     while(1) 
//     {
// 	    printf(1, "%d before picking up forks\n", i);
//         sem_acquire(i);
//         sem_acquire((i + 1) % 5);
    
//         printf(1, "%d picked up forks\n", i);

//         sem_release(i);
//         sem_release((i + 1) % 5);

//         printf(1, "%d released forks", i);
//     }
//     exit();
}